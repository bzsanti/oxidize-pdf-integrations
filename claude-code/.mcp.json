{
  "mcpServers": {
    "oxidize-pdf": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/launch-mcp",
      "args": ["serve"],
      "env": {
        "OXIDIZE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
